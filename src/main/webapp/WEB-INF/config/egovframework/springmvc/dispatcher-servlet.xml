<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context-4.0.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

    <context:component-scan base-package="egovframework">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>

    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <constructor-arg value="UTF-8"/>
            </bean>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="objectMapper">
                    <bean class="com.fasterxml.jackson.databind.ObjectMapper"/>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- =====================================================
         인터셉터 설정 - Role 기반 접근 제어
         ===================================================== -->
    <mvc:interceptors>

        <!-- 로그인 필수 URL -->
        <mvc:interceptor>
            <mvc:mapping path="/loan/myLoan.do"/>
            <mvc:mapping path="/loan/loanApply.do"/>
            <mvc:mapping path="/loan/loanApplyMulti.do"/>
            <mvc:mapping path="/loan/requestReturn.do"/>
            <mvc:mapping path="/loan/myLoanSummary.do"/>
            <bean class="egovframework.example.common.interceptor.AuthInterceptor"/>
        </mvc:interceptor>

        <!-- 어드민 전용 URL -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <mvc:mapping path="/book/bookRegisterView.do"/>
            <mvc:mapping path="/book/bookRegister.do"/>
            <mvc:mapping path="/book/bookModifyView.do"/>
            <mvc:mapping path="/book/bookModify.do"/>
            <mvc:mapping path="/book/bookDelete.do"/>
            <bean class="egovframework.example.common.interceptor.AdminInterceptor"/>
        </mvc:interceptor>

    </mvc:interceptors>

    <bean id="viewResolver" class="org.springframework.web.servlet.view.BeanNameViewResolver" p:order="0"/>
    <bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView"/>

    <bean id="internalViewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver"
          p:order="1"
          p:prefix="/WEB-INF/jsp/"
          p:suffix=".jsp"/>
</beans>
